/// Auto-generated by Deno Win32: Windows.Win32.Media.DirectShow.Xml.Apis

import * as util from "../../../util.ts";

// Structs

/**
 * Windows.Win32.Media.DirectShow.Xml.Apis (size: 8)
 */
export interface Apis {
  /** System.Guid */
  CLSID_XMLGraphBuilder: Uint8Array | Deno.PointerValue | null;
}

export const sizeofApis = 8;

export function allocApis(data?: Partial<Apis>): Uint8Array {
  const buf = new Uint8Array(sizeofApis);
  const view = new DataView(buf.buffer);
  // 0x00: pointer
  if (data?.CLSID_XMLGraphBuilder !== undefined) view.setBigUint64(0, data.CLSID_XMLGraphBuilder === null ? 0n : BigInt(util.toPointer(data.CLSID_XMLGraphBuilder)), true);
  return buf;
}

// Native Libraries

// Symbols

